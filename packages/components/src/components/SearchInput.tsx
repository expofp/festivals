import React from 'react';

export interface SearchInputProps {

}

const SearchInput = () => {
    return (
        <div>
            <input type="text" placeholder="Search venue, event, speaker" />
        </div>
    );
};

export default SearchInput;
